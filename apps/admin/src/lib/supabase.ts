import { getSupabaseBrowserClient, getSupabaseServerClient, getSupabaseServiceClient } from '@media-network/shared';
import type { SupabaseClient } from '@media-network/shared';

// Re-export for convenience within the admin app
export { getSupabaseBrowserClient, getSupabaseServerClient, getSupabaseServiceClient };
export type { SupabaseClient };
